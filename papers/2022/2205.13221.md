---
arxiv_id: "2205.13221"
title: "QSpeech: Low-Qubit Quantum Speech Application Toolkit"
authors:
  - Zhenhou Hong
  - Jianzong Wang
  - Xiaoyang Qu
  - Chendong Zhao
  - Wei Tao
  - Jing Xiao
submitted: "2022-05-26"
categories:
  - quant-ph
  - cs.LG
arxiv_url: https://arxiv.org/abs/2205.13221
source: arxiv-html
converter: pandoc
llm_remediated: false
citations_resolved: 0/0
citations_resolved_at: "2026-07-07T16:33:19+00:00"
references_parsed: 0
arxiv_version: ""
---

# QSpeech: Low-Qubit Quantum Speech Application Toolkit

1Corresponding author: Jianzong Wang, jzwang@188.com Zhenhou Hong, Jianzong Wang1, Xiaoyang Qu, Chendong Zhao, Wei Tao and Jing Xiao Ping An Technology (Shenzhen) Co., Ltd., Shenzhen, China\
Emails: {hongzhenhou168,wangjianzong347,quxiaoyang343,zhaochendong343,taowei085,xiaojing661}@pingan.com.cn

###### Abstract

Quantum devices with low qubits are common in the Noisy Intermediate-Scale Quantum (NISQ) era. However, Quantum Neural Network (QNN) running on low-qubit quantum devices would be difficult since it is based on Variational Quantum Circuit (VQC), which requires many qubits. Therefore, it is critical to make QNN with VQC run on low-qubit quantum devices. In this study, we propose a novel VQC called the low-qubit VQC. VQC requires numerous qubits based on the input dimension; however, the low-qubit VQC with linear transformation can liberate this condition. Thus, it allows the QNN to run on low-qubit quantum devices for speech applications. Furthermore, as compared to the VQC, our proposed low-qubit VQC can stabilize the training process more. Based on the low-qubit VQC, we implement QSpeech¹¹1Our implementation of QSpeech is publicly available at https://github.com/zhenhouhong/QSpeech, a library for quick prototyping of hybrid quantum-classical neural networks in the speech field. It has numerous quantum neural layers and QNN models for speech applications. Experiments on Speech Command Recognition and Text-to-Speech show that our proposed low-qubit VQC outperforms VQC and is more stable.

###### Index Terms:

Quantum Neural Networks Library, Hybrid Quantum-classical Neural Networks, Low-qubit, Variational Quantum Circuit, Speech Application

## I Introduction

Recent progress in the development and commercialization of quantum computation has significantly impacted the landscape of Quantum Neural Network (QNN) \[1, 2\]. These quantum algorithms designed for the Noisy Intermediate-Scale Quantum (NISQ) \[3\] computer are programmed with a Variational Quantum Circuit (VQC) \[4\] and a classical optimizer. The approach is similar to the parameter updating in many machine learning algorithms. The QNN has been developed by some researchers in various applications \[5, 6\].

The number of qubits in the NISQ era \[3\] is $`5`$ to $`50`$ qubits. Several quantum devices have less than 10 qubits. The application of quantum algorithms in these quantum devices is a key challenge. Mainstream research has focused on running the quantum algorithm in low-qubit ($`2`$ to $`10`$) quantum devices \[7, 8\]. Using a scalable trapped atomic ion system \[9\], the Grover Search algorithm \[7\] can be implemented on $`3`$ qubits quantum device. The Quantum Principal Component Analysis (QPCA) \[10\] is developed to the Resonant QPCA \[11\], which can be conducted in a low-qubit quantum device using a resonant analysis algorithm.

However, it can be challenging when QNN or hybrid quantum-classical neural networks are implemented in speech applications \[12, 13\]. First, these algorithms with VQC in speech applications need many qubits, making them difficult to implement. Second, with numerous qubits, QNN will have the Barren Plateau problem \[14, 15\] in the training process. When restricting the number of qubits, it is challenging to assure model performance.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x1.png)

\(a\) The framework of speech application with DNN.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x2.png)

\(b\) The framework of speech application with QNN.

Figure 1: The framework of speech application using DNN compared with QNN. The blue boxes in (a) represents the DNN modules, the orange boxes in (b) represents the QNN modules. The only difference is that DNN is replaced with QNN, with the same preprocessing and postprocessing.

In this study, we propose a novel VQC called low-qubit VQC that allows the QNN or hybrid quantum-classical neural networks to run in quantum devices with fewer qubits. Our proposed low-qubit VQC can be used in the neural network when high-dimensional units, such as 128 and 256, are required in the low-qubit quantum devices. Furthermore, to stabilize the convergence, we add an output clip operation in the first linear transformation in low-qubit VQC. Adding output clip operation can avoid gradient vanishing problems. Compared to VQC, the low-qubit VQC can enhance the convergence in training. Additionally, we propose a framework called QSpeech, which uses the QNN module for easy use in speech applications \[16, 17\]. QSpeech based on the PennyLane \[18\] and PyTorch \[19\] as backend, can define a quantum circuit in the function with a simple Python-based user interface. We implement the neural layer functions in QSpeech such as Quantum Convolution (QConv), quantum long short term memory \[20\], Quantum Gate Recurrent Unit (QGRU), and Quantum Attention (QAttention). Thus, the QSpeech can easily implement the hybrid quantum-classical neural network.

Our main contributions are as follows:

- •
  Low-qubit VQC has been proposed, allowing QNN to be used in a low-qubit quantum device. It can stabilize the training process compared to VQC.
- •
  Based on low-qubit VQC, we design a library for the rapid prototyping of hybrid quantum-classical neural networks in speech applications, named QSpeech.
- •
  In the experiments, hybrid quantum-classical neural networks were successfully applied in speech applications.

## II Related Works

### II-A Quantum Convolutional Neural Networks

Cong et al. \[21\] propose a quantum CNN based on a quantum circuit model that incorporates the ideas of convolutional and pooling layers from classical CNNs. The proposed architecture is similarly layered; however, it applies 1D convolutions to the input quantum state (contrary to 2D/3D convolutions on images). On the input state density $`\rho - {i\hspace{0pt}n}`$, the convolutional layer is modeled as a quasi-local unitary operation. This unitary operator, denoted by $`U_{i}`$, is applied to several successive sets of input qubits up to a predefined depth. The pooling layer is implemented by measuring some qubits and applying unitary rotations $`V_{i}`$ to the nearby qubits. The observations on the qubits determine the rotation operation. This combines the functionalities of dimensionality reduction (the output of $`V_{i}`$ is of lower dimension) and nonlinearity (due to the partial measurement of qubits). After the required number of convolutional blocks and pooling unitaries, the unitary $`F`$ implements the fully connected layer. A final measurement on the output of $`F`$ yields the network output.

Recently, Kerenidis et al. \[22\] identified the relationship between convolutions and matrix multiplications and proposed the first quantum algorithm to compute a CNN’s forward pass as a convolutional product. They also proposed a quantum backpropagation algorithm to learn network parameters through gradient descent. In an application of QCNNs, Zhang et al. \[23\] and Melnikov et al. \[24\] propose special convolutional neural networks for extracting features from graphs to identify those showing the quantum advantage. Hong et al. \[25\] propose a novel structure of QCNN that outperforms other DNN algorithms in predicting protein distance and contact.

### II-B Quantum Recurrent Neural Networks

There have also been several interesting suggestions for developing quantum variants of RNN. Hibat-Allah et al. \[26\] propose a quantum variant of RNNs using variational wave functions to learn the approximate ground state of a quantum Hamiltonian. Roth \[27\] propose an iterative retraining approach using RNNs for simulating bulk quantum systems via mapping translations of lattice vectors to the RNN time index. Hopfield Networks \[28\] were a popular early type of a recurrent NN for which quantum variants have been developed in several works \[29\].

### II-C Quantum Learning for Speech Processing

Li et al. \[30\] propose a QNN that combines fuzzy theoretic principles with continuous digits inference. Their results show that more than $`15\%`$ error reduction is obtained on a speaker-independent continuous digits recognition task. Li et al. \[31\] propose a QNN used in speech enhancement. The QNN can be trained on the backpropagation algorithm. Its simulation results show that the QNN outperforms the traditional spectral subtraction in speech enhancement. Fu et al. \[32\] propose an improved particle swarm optimization (IPSO), called IPSO-QNN, for training the QNN. Its experimental results in speech recognition are better than other approaches. Yang et al. show that the QCNN can use in privacy-preserving learning \[6\]. It uses the QCNN to encode the speech feature to quantum state for data protection.

## III Preliminaries

The entire quantum variational circuit \[4\] consists of three components: Data Encoding Layer, Variational Layer, and Quantum Measurement Layer. Notably, the number of qubits and measurements can be changed to fit the problem of interest, and the variational layer can contain many dashed boxes to increase the number of parameters, all subject to the capacity and capability of the quantum machines used in the experiments.

Data Encoding Layer. We first encode a classical input vector into a quantum state in this layer, which is required for further processing. A general $`N`$-qubit quantum state can be represented as the following:

```math
\left| \phi \right\rangle = {\sum\limits_{{(q_{1},q_{2},\ldots,q_{N})} \in {\{ 0,1\}}^{N}}{{c_{q_{1},\ldots,q_{N}}\hspace{0pt}\left| q_{1} \right\rangle} \otimes \left| q_{2} \right\rangle \otimes \left| q_{3} \right\rangle \otimes \ldots \otimes \left| q_{N} \right\rangle}}
```

where $`c_{q_{1},\ldots,q_{N}} \in {\mathbb{C}}`$ is the amplitude of each quantum state and $`q_{i} \in \left\{ 0,1 \right\}`$. The square of the amplitude $`c_{q_{1},\ldots,q_{N}}`$ is the probability of measurement with the post-measurement state in $`\left| q_{1} \right\rangle \otimes \left| q_{2} \right\rangle \otimes \left| q_{3} \right\rangle \otimes \ldots \otimes \left| q_{N} \right\rangle`$, and the total probability should sum to 1, i.e.,

```math
{\sum\limits_{{(q_{1},q_{2},\ldots,q_{N})} \in {\{ 0,1\}}^{N}}\left\| c_{q_{1},\ldots,q_{N}} \right\|^{2}} = 1
```

For the quantum gates, the input will first be flattened and transformed into rotation angles. In this study, $`f_{e}\hspace{0pt}{( \cdot )}`$ represent the data encoder.

Variational Layer. After encoding the classical values into a quantum state, it will be subject to a series of unitary transformations. The variational layer consists of two parts, one for entanglement and the other for rotation, which include numerous single-qubit unitary rotations $`U`$ parameterized by $`1`$ to $`m`$ parameters $`\theta_{1},\ldots,\theta_{m}`$. The parameters labeled by $`\theta_{1},\ldots,\theta_{m}`$ are the ones that will be updated by the optimization procedure. In this study, $`f_{u}\hspace{0pt}{(\theta_{1},\ldots,\theta_{m}; \cdot )}`$ represent the variational circuit.

Quantum Measurement(Decode) Layer. We performed a quantum measurement layer to obtain the transformed data from VCs. The VCs’ ensemble samplings (expectation values) were considered. Quantum simulation software (for example, PennyLane or IBM Qiskit) can be used to calculate this value deterministically. While implementing on a real quantum computer, it is necessary to prepare the same system and repeatedly use the measurements to gain adequate statistics. In this study, $`f_{d}\hspace{0pt}{( \cdot )}`$ represent the measurement layer.

## IV Proposed Methods

### IV-A Challenges of VQC in Low-qubit Quantum Device

The VQC consists of data encoder, variational circuit, and measurement \[33, 4\]. It only processes the 1D array and necessitates qubits based on the size of one dimension input. It is difficult to use the VQC in a high-dimensional input $`\mathbf{x} \in {\mathbb{R}}^{1 \times n}`$ ($`n`$ is 128 or 256 or even larger) because it requires $`n`$ qubits to encode and transform the input in VQC, which exceeds the number of qubits. Furthermore, with multiple qubits, it will be easy to cause the Barren Plateaus problem \[14, 15\] during the training process.

### IV-B Problem Statement

Similar to deep learning \[34, 35, 36\] in speech application, using the QNN $`f_{Q}\hspace{0pt}{( \cdot )}`$ to model a function can generate $`\mathbf{y}`$, which represents the classify of audio or generate speech from data input $`\mathbf{x}`$.

$`\mathbf{y} = {f_{q}\hspace{0pt}{(\mathbf{W},{f_{v}\hspace{0pt}{(\theta)}},\mathbf{x})}}`$ $`{\mathcal{L}\hspace{0pt}{(\mathbf{W},{f_{v}\hspace{0pt}{(\theta)}},\mathbf{x},\mathbf{y}_{t\hspace{0pt}r\hspace{0pt}u\hspace{0pt}e})}} = {\mathcal{L}\hspace{0pt}{({f_{q}\hspace{0pt}{(\mathbf{W},{f_{v}\hspace{0pt}{(\theta)}},\mathbf{x})}},\mathbf{y}_{t\hspace{0pt}r\hspace{0pt}u\hspace{0pt}e})}}`$

In VQC $`f_{v}\hspace{0pt}{(\theta)}`$, we use the RY gate $`R_{y}\hspace{0pt}{( \cdot )}`$ in Data Encoding Layer. $`\mathbf{y}_{t\hspace{0pt}r\hspace{0pt}u\hspace{0pt}e}`$ is the true label in data.

The optimization for QNN in speech application, is similar with DNN:

$`\hat{\theta} = {\underset{\theta}{\arg\min}{\mathcal{L}\hspace{0pt}{({f_{q}\hspace{0pt}{(\mathbf{W},{f_{V}\hspace{0pt}{(\theta)}},\mathbf{x})}},\mathbf{y}_{t\hspace{0pt}r\hspace{0pt}u\hspace{0pt}e})}}}`$$`\hat{\mathbf{W}} = {\underset{\mathbf{W}}{\arg\min}{\mathcal{L}\hspace{0pt}{({f_{q}\hspace{0pt}{(W,{f_{v}\hspace{0pt}{(\theta)}},\mathbf{x})}},\mathbf{y}_{t\hspace{0pt}r\hspace{0pt}u\hspace{0pt}e})}}}`$

where the $`\hat{\theta}`$ and $`\hat{\mathbf{W}}`$ are optimized parameters in $`f_{q}\hspace{0pt}{(\mathbf{W},{f_{v}\hspace{0pt}{(\theta)}},\mathbf{x})}`$. Based on PennyLane setting, VQC $`f_{v}\hspace{0pt}{(\theta)}`$ can perform the optimization using gradient descent approaches. In this study, we follow the \[37\], using the Adam optimization method \[38\] to minimize the loss $`\mathcal{L}\hspace{0pt}{({f_{q}\hspace{0pt}{(\mathbf{W},{f_{v}\hspace{0pt}{(\theta)}},\mathbf{x})}},{a\hspace{0pt}n\hspace{0pt}d\hspace{0pt}\mathbf{y}_{t\hspace{0pt}r\hspace{0pt}u\hspace{0pt}e}})}`$.

### IV-C Low-qubit Variational Quantum Circuit(VQC)

As shown in Fig. 2 ‣ IV Proposed Methods ‣ QSpeech: Low-Qubit Quantum Speech Application Toolkit"), the two purple blocks are the new parts that we added in VQC. In low-qubit VQC, we add two linear transformations unlike in previous VQC \[21, 20\],.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x3.png)

Figure 2: The proposed low-qubit VQC. The “LT” is the linear transformation, “RY” is the rotation gates along the y axis, $`U{(\theta_{1},..,\theta_{m})}`$ is the single-qubit unitary rotations $`U`$ parameterized by $`1`$ to $`m`$ parameters $`\theta_{1},\ldots,\theta_{m}`$.

Consider $`\mathbf{x}`$ as input, let $`\mathbf{x} = {\{ x_{1},x_{2},\ldots,x_{n}\}}`$, $`\mathbf{x} \in {\mathbb{R}}^{1 \times n}`$. LT $`f_{l\hspace{0pt}t}\hspace{0pt}{(\mathbf{W}; \cdot )}`$ is linear transformation, $`\mathbf{W}`$ is the learnable parameter in $`f_{l\hspace{0pt}t}\hspace{0pt}{(\mathbf{W}; \cdot )}`$. $`{f_{l\hspace{0pt}t}\hspace{0pt}{(\mathbf{W}; \cdot )}}:{\mathbf{x}\rightarrow\mathbf{y}}`$. $`\mathbf{q}_{1}`$, $`\mathbf{q}_{2}`$, and $`\mathbf{q}_{3}`$ are representative quantum states. $`\mathbf{y}_{1}`$ and $`\mathbf{y}_{2}`$ are the outputs of first and second $`f_{l\hspace{0pt}t}`$. $`\mathbf{y} = {\{ y_{1},y_{2},\ldots,y_{j}\}}`$. Then the formula of low-qubit VQC follows:

$`\mathbf{y}_{1} = {f_{l\hspace{0pt}t}^{1}\hspace{0pt}{(\mathbf{W};\mathbf{x})}}`$$`\mathbf{q}_{1} = {f_{e}\hspace{0pt}{(\mathbf{y}_{1})}}`$$`\mathbf{q}_{2} = {f_{u}\hspace{0pt}{(\theta_{1},\ldots,\theta_{m};{c\hspace{0pt}{(\mathbf{q}_{1})}})}}`$$`\mathbf{q}_{3} = {f_{d}\hspace{0pt}{(\mathbf{q}_{2})}}`$$`\mathbf{y}_{2} = {f_{l\hspace{0pt}t}^{2}\hspace{0pt}{(\mathbf{W};\mathbf{q}_{3})}}`$

Where $`\theta_{1},\ldots,\theta_{m}`$ is the parameters in $`U\hspace{0pt}{(\theta; \cdot )}`$, $`m`$ is a positive integer. $`f_{e}`$, $`f_{u}`$, and $`f_{d}`$ are the data encoder, variational circuit, and measurement operation. $`c\hspace{0pt}{( \cdot )}`$ is the CNOT gate. $`f_{l\hspace{0pt}t}^{1}`$ and $`f_{l\hspace{0pt}t}^{2}`$ is the first and second linear transformation.

The advantage of adding the linear transformation is that it can squeeze high-dimensional input in the data encoding layer or expand the output filters after measurement. Several qubits are not required since they will be squeezed with the linear transformation before the data encoding and variational layers. It can make convergence stable in the training and need fewer resources to simulate the quantum computation. Additionally, it makes low-qubit VQC process multiple channels simultaneously while extracting the features from input. It is beneficial for the QConv and QAttention layers. The QAttention layer can allow high-dimensional units such as 128 or 256 with few qubits. Thus, the Transformer with QAttention can be used in the low-qubit quantum devices for speech applications \[39\] such as Text-to-Speech (TTS).

### IV-D Analysis of the Low-qubit VQC

We found the $`f_{e}\hspace{0pt}{(\mathbf{y}_{1})}`$ contains the $`\arctan{( \cdot )}`$, its derivative may cause the gradient vanish. $`{f_{e}\hspace{0pt}{(\mathbf{y}_{1})}} = {R_{y}\hspace{0pt}{({\arctan{(y_{11})}})}\hspace{0pt}\left| 0 \right\rangle\hspace{0pt}\ldots\hspace{0pt}R_{y}\hspace{0pt}{({\arctan{(y_{1\hspace{0pt}i})}})}\hspace{0pt}\left| 0 \right\rangle}`$. Its derivative results of $`R_{y}\hspace{0pt}{({\arctan{(y_{1\hspace{0pt}i})}})}`$ as follow:

$`\frac{\partial{R_{y}\hspace{0pt}{({\arctan{(y_{1\hspace{0pt}i})}})}}}{\partial w_{i}} = {- {R_{y}\hspace{0pt}{({\arctan{(y_{1\hspace{0pt}i})}})}^{\intercal}\hspace{0pt}\frac{\partial{\arctan{(y_{1\hspace{0pt}i})}}}{\partial w_{i}}}}`$$`\frac{\partial{\arctan{(y_{1\hspace{0pt}i})}}}{\partial w_{i}} = {\frac{1}{1 + {(y_{1\hspace{0pt}i})}^{2}}\hspace{0pt}\frac{\partial y_{1\hspace{0pt}i}}{\partial w_{i}}} = {\frac{1}{1 + {(y_{1\hspace{0pt}i})}^{2}}\hspace{0pt}x_{i}}`$

Where $`y_{1\hspace{0pt}i}`$ is the element in $`\mathbf{y}_{1}`$. $`y_{1\hspace{0pt}i} = {\mathbf{w}\mathbf{x}}`$. $`\mathbf{w}`$ is a vector in $`\mathbf{W}`$.

We analysis the $`x_{i}/{({1 + {(y_{i})}^{2}})}`$. To observe the output, we fix the $`y_{i}`$ and change the $`x_{i}`$. We can see that the $`y_{i}`$ cannot be too big or too small, or the gradient will vanish. Therefore, we add the output clip operation in the first linear transformation. It is possible to optimize the weight of linear transformation. The output clip’s range is $`\lbrack{- 3},3\rbrack`$.

The LT with learnable parameters can be implemented using FC. We compare the linear transformation with other subsampling approaches like bilinear interpolation. The linear transformation has two advantages: first, it can reduce the dimension and extract the features efficiently with learnable parameters; second, it can extract the features adapt to the VQC with learnable parameters. Bilinear interpolation simply reduces the dimension, causing the gradient vanish based on the derivative result.

### IV-E Framework of QSpeech

Fig. 3 shows the software architecture of QSpeech. In the QSpeech, the main parts of QNN training and inference are written in python, which calls PennyLane and PyTorch by switching the backend option. PennyLane allows differentiable programming of quantum computers. We also provide complete recipes for conducting speech application experiments. As shown in Fig. 3, the QSpeech is based on the PyTorch and PennyLane. The functional or layers backend are qcircuit, qconv, qlstm, qgru, and qattention. The functional backend builds up the model libraries: Quantum Transformer-TTS (QTransformer-TTS), Quantum M5 (QM5), and Quantum Tacotron (QTacotron). The Quantum Layers (QLayers) are the functional backend, and the Quantum Models (QModels) are the model libraries in the major libraries in QSpeech. Using the PennyLane as a Quantum Machine Learning (QML) backend can easily embed the code into Python and PyTorch.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x4.png)

\

Figure 3: The software architecture of QSpeech. All functional backends consists of low-qubit VQC.

## V Implemented Quantum Neural Network in Speech Application

### V-A Quantum M5 For SCR

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x5.png)

\(a\) M5 model.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x6.png)

\(b\) QM5 model.

Figure 4: The model architecture of QM5 compared with M5.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x7.png)

Figure 5: The process of QConv1d operation. The $`a_{1},a_{2},\ldots,a_{n}`$ means the value of input data. The $`f_{1},f_{2},\ldots,f_{n}`$ means the feature maps.

The M5 \[40\] model is a deep convolutional neural network (CNN) that directly uses time-domain waveforms as inputs. To extract the feature from the raw speech data, the M5 model in speech command uses one dimension convolutional layer (Conv1d). Thus, we first implement the quantum one dimension convolutional (QConv1d) layer that can use in speech commands and other speech applications \[41, 42\]. Then, we use the M5 model with the QConv1d layer in the speech command task, called quantum M5(QM5).

Fig. 5 shows the QConv1d extract feature from raw speech data or feature map. We propose a QConv1d that uses the low-qubit VQC and help extract from a feature map, while avoiding source in the quantum simulator.

### V-B Quantum Tacotron For TTS

Tacotron \[43\] is an end-to-end generative TTS model that synthesizes speech directly from characters. It has an Attention Decoder part, which includes a pre-net, an attention recurrent neural networks (RNN), and a decoder RNN. The RNN component uses the GRU. As shown in Fig. 6, we change the original GRU to QGRU in attention GRU and decoder GRU. The others are maintained.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x8.png)

\(a\) Attention decoder part in Tacotron.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x9.png)

\(b\) Attention decoder part in QTacotron.

Figure 6: The attention decoder with GRU compared with the attention decoder with quantum GRU.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x10.png)

\

Figure 7: The proposed QGRU architecture. Each low-qubit VQC as detailed in Figure 2 ‣ IV Proposed Methods ‣ QSpeech: Low-Qubit Quantum Speech Application Toolkit"). The sigmoid and tanh blocks represent the sigmoid and the hyperbolic tangent activation function, respectively. $`X_{t}`$ is the input at time $`t`$, $`H_{t}`$ is for the hidden state, $`Y_{t}`$ is the output. “DotMul” represents element-wise multiplication.

To construct the basic unit of the proposed QGRU architecture, a QGRU cell, we stack the low-qubit VQC blocks as mentioned earlier together. In Fig. 7, each of the low-qubit VQC is described in the previous section (see also Fig. 2 ‣ IV Proposed Methods ‣ QSpeech: Low-Qubit Quantum Speech Application Toolkit")). In a QGRU cell, there are five low-qubit VQC. For three low-qubit VQC in Fig. 7 below, the input is the concatenation $`v_{t}`$ of the hidden state $`H_{t - 1}`$ from the previous time step and the current input vector $`X_{t}`$, and the output is three vectors obtained from the measurements at the end of each low-qubit VQC. The output value can then be further processed with two low-qubit VQC in Fig. 7 top right, to get $`Y_{t}`$ and $`H_{t}`$.

### V-C Quantum Transformer For TTS

Transformer-TTS \[44\] is an architecture that uses two parts (Encoder and Decoder) to transform one sequence into another. Here we implement the quantum self-attention with a single head and multihead. As shown in Fig. 8(a), adding two VQC blocks in the input and output, others are the same with Scaled-Dot Product Attention \[44\]. The quantum self-attention with multihead as shown in Fig. 8(b), the $`Q`$, $`V`$, $`K`$ computed by the VQC, input to the quantum self-attention with a single head. The final output is also computed by the VQC. The others are the same as the Multihead Attention \[44\].

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x11.png)

\(a\) Quantum self-attention with single head.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x12.png)

\(b\) Quantum self-attention with multihead.

Figure 8: The proposed quantum self-attention architecture with single head and multihead. The low-qubit VQC as shown in Figure 2 ‣ IV Proposed Methods ‣ QSpeech: Low-Qubit Quantum Speech Application Toolkit").

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x13.png)

Figure 9: The proposed QTransformer-TTS architecture. The quantum self-attention with multihead as shown in Figure 8(b).

With the quantum self-attention with multihead, the quantum Transformer-TTS (QTransformer-TTS) will also be implemented. As shown in Fig. 9, change the Multihead Attention to the quantum self-attention with multihead.

|       |               |                  |            |               |               |
| ----- | ------------- | ---------------- | ---------- | ------------- | ------------- |
| Model | Type of VQC   | Number of qubits | Batch size | Test accuracy | Training time |
| M5    | \-            | \-               | 64         | 63.75%        | \-            |
|       | \-            | \-               | 256        | 61.53%        | \-            |
| QM5   | VQC           | 2                | 64         | 30.57%        | 741s          |
|       | VQC           | 2                | 256        | 29.13%        | 2905s         |
|       | Low-qubit VQC | 2                | 64         | 47.69%        | 79s           |
|       | Low-qubit VQC | 2                | 256        | 45.61%        | 278s          |
|       | VQC           | 4                | 64         | 32.61%        | 1659s         |
|       | VQC           | 4                | 256        | 30.18%        | 6601s         |
|       | Low-qubit VQC | 4                | 64         | 48.50%        | 142s          |
|       | Low-qubit VQC | 4                | 256        | 47.03%        | 550s          |
|       | VQC           | 8                | 64         | 33.93%        | 3521s         |
|       | VQC           | 8                | 256        | 31.74%        | 13273s        |
|       | Low-qubit VQC | 8                | 64         | 50.11%        | 346s          |
|       | Low-qubit VQC | 8                | 256        | 48.97%        | 1265s         |

TABLE I: The Results of QM5 Compared with M5. In QM5, compared low-qubit VQC with VQC in different qubits.

## VI Experiments

### VI-A Dataset

LJSpeech \[45\]. This dataset consists of 13,100 short audio clips of a single speaker reading passages from seven nonfiction books. The texts are normalized and inserted with the beginning character (space) and end character (a period), e.g., “There are 16 apples” is converted to“ there are sixteen apples.”. The LJSpeech dataset is randomly divided into two sets: 12,600 samples for training and 500 samples for testing.

Google Speech Command-V2 \[46\]. For spoken word recognition, we use the 35 class setting, including the following frequent speech commands: ‘left’, ‘go’, ‘yes’, ‘down’, ‘up’, ‘on’, ‘right’, ‘no’, ‘off’, ‘stop’, and so on with a total of 84,843 training examples and 11,005 testing examples.

### VI-B Settings

Quantum Environment. The simulation of quantum computation runs on the CPU. We limit the number of qubits to small numbers (2 to 10). The $`m`$ of $`\theta_{1},\ldots,\theta_{m}`$ is equal to 3 in VQC and low-qubit VQC.

Model Architecture. The QTransformer-TTS has two layers. It replaces self-attention with QAttention, the other settings follow \[44\]. The QGRU is a substitute for GRU in QTacotron. QTacotron has same the same parameter as as \[43\]. The QM5 replaces the 1D convolution (Conv1d) with 1D QConv (QConv1d). The QTransformer-TTS, QM5, and QTacotron use the low-qubit VQC. As we discussed in Sec. IV-C ‣ IV Proposed Methods ‣ QSpeech: Low-Qubit Quantum Speech Application Toolkit"), VQC requires similar qubits as the input dimensions, and it is considered large for quantum devices. Therefore, the low-qubit VQC in QTransformer-TTS and QTacotron, cannot be compared with VQC. The QConv1d with VQC or low-qubit VQC of QM5, in which the number of qubits is the same as the kernel size, follows the \[21, 20\]. The linear transformation in low-qubit VQC forms a fully connected layer.

Training. Adam is the optimizer in all experiments, with $`\beta_{1} = 0.9`$, $`\beta_{2} = 0.99`$. The batch size in the training process of QTransformer-TTS and QTacotron is $`2`$. QM5 can be trained in a large batch size (e.g., $`64`$, $`256`$) using QConv1d with our proposed low-qubit VQC.

Compared Schemes. In the experiments, the QTransformer-TTS, QM5, and QTacotron models are compared with the original Transformer-TTS, M5, and Tacotron. The setups of training for the QTransformer-TTS, QM5, and QTacotron are the same as the original Transformer-TTS, M5, and Tacotron. It will also compare the results of different qubits in the experiments of QTransformer-TTS, QM5, and QTacotron.

Evaluation. In Speech Command Recognition (SCR) experiments, the metric of accuracy is the same as \[40\]. We use the mean opinion score (MOS) to measure generated voices’ qualities in TTS experiments. Each sentence is judged by ten males and ten females native English speakers.

### VI-C Results and Analysis

QM5. In Fig. 10(a), the yellow curve is the training loss of QM5 with VQC. Notably, it cannot stably converge as the iterative process increases. The black curve, which is the QM5 with low-qubit VQC, can train in convergence and stability, indicating that using low-qubit VQC can handle the Barren Plateaus challenge. In Fig. 10(b), the training of QM5 with low-qubit VQC can improve convergence and stability compared to the M5. With more qubits in QM5, the QM5 model will converge faster.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x14.png)

\(a\) Loss of VQC and low-qubit VQC in QM5.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x15.png)

\(b\) Loss of M5 and QM5(with low-qubit VQC).

Figure 10: The loss curves of M5 and QM5. (a) The training losses of QM5 with VQC and low-qubit VQC. (b) The training loss curve of QM5 with different numbers of qubits, compared with M5.

In Table I, with additional qubits, the QM5 obtains better performance. Compared with VQC, the low-qubit VQC is $`16\%`$ higher. Table I shows that the low-qubit VQC is faster than the VQC. For the same batch size, the low-qubit VQC can reduce the training by $`10`$ times compared to the VQC. Table I also shows the accuracy between the M5 and the QM5. The QM5, when using the low-qubit VQC, can gain $`16\%`$ improvement in accuracy compared to the VQC, indicating that the QM5 can work in the SCR task.

As shown in Fig. 11, the QConv1d can extract the feature from an input like Conv1d. The feature pattern extracted by QConv1d is similar to that by Conv1d.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x16.png)

\(a\) Feature map of M5.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x17.png)

\(b\) Feature map of QM5.

Figure 11: The feature map output from M5 and QM5.(a) The feature map output from Conv1d in M5.(b) The feature map output from QConv1d in QM5.

QTacotron. As shown in Fig. 12(a), the QTacotron with the low-qubit VQC converges faster in training than Tacotron (the blue curve), indicating that the low-qubit VQC can operate well in QGRU, like the low-qubit VQC in QConv1d. The $`0\%`$ to $`20\%`$ of the training process indicates that the QTacotron convergence is faster than that of the QTacotron. Following the training steps, the QTacotron has less oscillation than the Tacotron. In Fig. 12(a), QTacotron with different qubits has a similar loss convergence. However, it shows that with more qubits, the QTacotron can converge better and with less loss.

|                  |               |          |      |
| ---------------- | ------------- | -------- | ---- |
| Model            | Type of VQC   | \#Qubits | MOS  |
| Tacotron         | \-            | \-       | 3.09 |
| QTacotron        | Low-qubit VQC | 2        | 2.22 |
|                  | Low-qubit VQC | 4        | 2.56 |
|                  | Low-qubit VQC | 8        | 2.67 |
| Transformer-TTS  | \-            | \-       | 2.91 |
| QTransformer-TTS | Low-qubit VQC | 2        | 2.19 |
|                  | Low-qubit VQC | 4        | 2.43 |
|                  | Low-qubit VQC | 8        | 2.52 |

TABLE II: Results of Tacotron, QTacotron, Transformer-TTS, and QTransformer-TTS. QTacotron and QTransformer-TTS run in different numbers of qubits with low-qubit VQC.

Table II shows the MOS with Tacotron and QTacotron. QTacotron’s performance will improve with more qubits of the low-qubit VQC. Therefore, the QTacotron can achieve better results when using the low-qubit VQC. With eight qubits of the low-qubit VQC, the MOS of QTacotron is about $`2.67`$, indicating that the QTacotron with low-qubit VQC can run in the TTS task.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x18.png)

\(a\) Loss of Tacotron and QTacotron.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x19.png)

\(b\) Loss of Transformer-TTS and QTransformer-TTS.

Figure 12: The loss curves of Tacotron, QTacotron, Transformer-TTS, and QTransformer-TTS. For QTacotron and QTransformer-TTS, both are compared with different number of qubits. (a) The loss curves of Tacotron and QTacotron. (b) The loss curves of Transformer-TTS and QTransformer-TTS.

In addition to the training process and performance, we provide the mel-spectrogram obtained from the Tacotron and QTacotron. As shown in Fig. 13(a)(b), similar mel-spectrograms are obtained from the Tacotron and QTacotron. We can observe that the QTacotron with low-qubit can achieve the approximate performance as Tacotron.

QTransformer-TTS. In the training process, QTransformer-TTS outperforms Transformer-TTS, as shown in Fig. 12(b). Compared to the training loss curve in Fig. 12(b), the QTransformer-TTS converges faster than the Transformer-TTS between $`0\%`$ and $`40\%`$ of the training process. Furthermore, QTransformer-TTS, like the QTacotron, gives similar loss convergence in different qubits. With more qubits of the low-qubit VQC, the QTransformer-TTS obtains lower loss in the training process, indicating that the low-qubit VQC can work well in QAttention, like the low-qubit VQC in QConv1d and QGRU.

Table II also shows the MOS with Transformer-TTS and QTransformer-TTS. We can see that QTransformer-TTS performs better when using the low-qubit VQC. With more qubits of the low-qubit VQC, QTransformer-TTS will get better performance. With eight qubits of the low-qubit VQC, the MOS of QTransformer-TTS is about $`2.52`$. It means that the QTransformer-TTS with low-qubit VQC can run in the TTS task with a low-qubit quantum device.

Furthermore, in Fig. 13(c)(d), we show the mel-spectrogram generated by the Transformer-TTS and QTransformer-TTS. The mel-spectrogram of the Transformer-TTS and QTransformer-TTS is similar. We conclude that QTransformer-TTS with low-qubit VQC can achieve the approximate performance as Transformer-TTS.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x20.png)

\(a\) Mel-spectrogram of Tacotron.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x21.png)

\(b\) Mel-spectrogram of QTacotron.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x22.png)

\(c\) Mel-spectrogram of Transformer-TTS.

![Refer to caption](https://ar5iv.labs.arxiv.org/html/2205.13221/assets/x23.png)

\(d\) Mel-spectrogram of QTransformer-TTS.

Figure 13: The mel-spectrogram between the Tacotron, QTacotron, Transformer-TTS, and QTransformer-TTS. (a) Mel-spectrogram generate by Tacotron. (b) Mel-spectrogram generate by QTacotron. (c) Mel-spectrogram generate by Transformer-TTS. (d) Mel-spectrogram generate by QTransformer-TTS.

Ablation Study. The low-qubit VQC is compared in two scenarios: first, the LT is implemented by FC layer or bilinear interpolation; second, the LT layer with or without output clip operation. Table III shows the results.

| Model            | Methods of LT              | Results |
| ---------------- | -------------------------- | ------- |
| QM5              | use bilinear interpolation | 37.51%  |
|                  | use FC                     | 43.41%  |
| QTacotron        | use bilinear interpolation | 2.11    |
|                  | use FC                     | 2.47    |
| QTransformer-TTS | use bilinear interpolation | 2.24    |
|                  | use FC                     | 2.32    |
| QM5              | without output clip        | 43.21%  |
|                  | with output clip           | 50.11%  |
| QTacotron        | without output clip        | 2.41    |
|                  | with output clip           | 2.67    |
| QTransformer-TTS | without output clip        | 2.39    |
|                  | with output clip           | 2.52    |

TABLE III: Compared results of QM5, QTacotron, and QTransformer-TTS. QTacotron and QTransformer-TTS run with low-qubit VQC in different settings.

Implemented methods of LT. In QM5, QTacotron, and QTransformer-TTS, the LT is implemented by FC layer or the LT implemented by bilinear interpolation. As shown in Table III, the results of using the FC layer are better than bilinear interpolation. In low-qubit VQC, using the FC layer outperforms the bilinear interpolation when implementing the LT.

With or without output clip. In QM5, QTacotron, and QTransformer-TTS, the results of LT with output clip operation are compared with LT without output clip operation. As shown in Table III shows, the results of using output clip are better than without output clip. In low-qubit VQC, the results of LT with output clip operation outperform LT without output clip operation.

## VII Conclusion

This study introduced a novel VQC, called low-qubit VQC. It can address the Barren Plateaus challenge and lower the requirement of qubits compared to VQC. It is crucial because VQCs are used in low-qubit quantum devices in the NISQ era. Low-qubit VQC can be used to implement QNN models in low-qubit quantum devices. Based on low-qubit VQC, we propose the QSpeech toolkit. QSpeech fully uses PyTorch and PennyLane as a QML engine and significantly simplifies the implementation and training of the entire speech application. Our QSpeech provided various quantum neural layers and hybrid quantum-classical neural networks. The experimental results showed that the hybrid quantum-classical neural networks with low-qubit VQC can be successfully used in speech applications.

## Acknowledgment

This paper is supported by the Key Research and Development Program of Guangdong Province under grant No.2021B0101400003. Corresponding author is Jianzong Wang from Ping An Technology (Shenzhen) Co., Ltd (jzwang@188.com).

## References

- \[1\] D. Wang, A. Sundaram, R. Kothari, A. Kapoor, and M. Roetteler, “Quantum algorithms for reinforcement learning with a generative model,” in _International Conference on Machine Learning_.   PMLR, 2021, pp. 10 916–10 926.
- \[2\] T. Birdal, V. Golyanik, C. Theobalt, and L. J. Guibas, “Quantum permutation synchronization,” in _Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition_, 2021, pp. 13 122–13 133.
- \[3\] J. Preskill, “Quantum computing in the nisq era and beyond,” _Quantum_, vol. 2, p. 79, 2018.
- \[4\] M. Benedetti, E. Lloyd, S. Sack, and M. Fiorentini, “Parameterized quantum circuits as machine learning models,” _Quantum Science and Technology_, vol. 4, no. 4, p. 043001, 2019.
- \[5\] A. Bisarya, W. El Maouaki, S. Mukhopadhyay, N. Mishra, S. Kumar, B. K. Behera, P. K. Panigrahi, and D. De, “Breast cancer detection using quantum convolutional neural networks: A demonstration on a quantum computer,” _medRxiv_, 2020.
- \[6\] C.-H. H. Yang, J. Qi, S. Y.-C. Chen, P.-Y. Chen, S. M. Siniscalchi, X. Ma, and C.-H. Lee, “Decentralizing feature extraction with quantum convolutional neural network for automatic speech recognition,” in _ICASSP 2021-2021 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP)_.   IEEE, 2021, pp. 6523–6527.
- \[7\] M. A. Nielsen and I. Chuang, “Quantum computation and quantum information,” 2002.
- \[8\] I. Tavernelli, A. Robert, P. Barkoutsos, and S. Woerner, “Resource-efficient quantum algorithm for protein folding,” _Bulletin of the American Physical Society_, vol. 65, 2020.
- \[9\] C. Figgatt, D. Maslov, K. A. Landsman, N. M. Linke, S. Debnath, and C. Monroe, “Complete 3-qubit grover search on a programmable quantum computer,” _Nature communications_, vol. 8, no. 1, pp. 1–9, 2017.
- \[10\] S. Lloyd, M. Mohseni, and P. Rebentrost, “Quantum principal component analysis,” _Nature Physics_, vol. 10, no. 9, pp. 631–633, 2014.
- \[11\] Z. Li, Z. Chai, Y. Guo, W. Ji, M. Wang, F. Shi, Y. Wang, S. Lloyd, and J. Du, “Resonant quantum principal component analysis.” _Science Advances_, vol. 7, no. 34, 2021.
- \[12\] X. Qu, J. Wang, and J. Xiao, “Evolutionary algorithm enhanced neural architecture search for text-independent speaker verification,” _Proc. Interspeech 2020_, pp. 961–965, 2020.
- \[13\] N. Zhang, J. Wang, W. Wei, X. Qu, N. Cheng, and J. Xiao, “Cacnet: Cube attentional cnn for automatic speech recognition,” in _2021 International Joint Conference on Neural Networks (IJCNN)_.   IEEE, 2021, pp. 1–7.
- \[14\] J. R. McClean, S. Boixo, V. N. Smelyanskiy, R. Babbush, and H. Neven, “Barren plateaus in quantum neural network training landscapes,” _Nature communications_, vol. 9, no. 1, pp. 1–6, 2018.
- \[15\] C. Zhao and X.-S. Gao, “Analyzing the barren plateau phenomenon in training quantum neural networks with the zx-calculus,” _Quantum_, vol. 5, p. 466, 2021.
- \[16\] Z. Hong, J. Wang, X. Qu, J. Liu, C. Zhao, and J. Xiao, “Federated Learning with Dynamic Transformer for Text to Speech,” in _Proc. Interspeech 2021_, 2021, pp. 3590–3594.
- \[17\] C. Zhao, J. Wang, X. Qu, H. Wang, and J. Xiao, “r-g2p: Evaluating and enhancing robustness of grapheme to phoneme conversion by controlled noise introducing and contextual information incorporation,” in _ICASSP 2022-2022 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP)_.   IEEE, 2022, pp. 6197–6201.
- \[18\] V. Bergholm, J. Izaac, M. Schuld, C. Gogolin, M. S. Alam, S. Ahmed, J. M. Arrazola, C. Blank, A. Delgado, S. Jahangiri _et al._, “Pennylane: Automatic differentiation of hybrid quantum-classical computations,” 2018.
- \[19\] A. Paszke, S. Gross, F. Massa, A. Lerer, J. Bradbury, G. Chanan, T. Killeen, Z. Lin, N. Gimelshein, L. Antiga _et al._, “Pytorch: An imperative style, high-performance deep learning library,” _Advances in Neural Information Processing Systems_, vol. 32, pp. 8026–8037, 2019.
- \[20\] S. Y.-C. Chen, S. Yoo, and Y.-L. L. Fang, “Quantum long short-term memory,” _Bulletin of the American Physical Society_, vol. 66, 2021.
- \[21\] I. Cong, S. Choi, and M. D. Lukin, “Quantum convolutional neural networks,” _Nature Physics_, vol. 15, no. 12, pp. 1273–1278, 2019.
- \[22\] I. Kerenidis, J. Landman, and A. Prakash, “Quantum algorithms for deep convolutional neural networks,” in _International Conference on Learning Representations_, 2019.
- \[23\] Z. Zhang, D. Chen, J. Wang, L. Bai, and E. R. Hancock, “Quantum-based subgraph convolutional neural networks,” _Pattern Recognition_, vol. 88, pp. 38–49, 2019.
- \[24\] A. A. Melnikov, L. E. Fedichkin, and A. Alodjants, “Predicting quantum advantage by quantum walk with convolutional neural networks,” _New Journal of Physics_, vol. 21, no. 12, p. 125002, 2019.
- \[25\] Z. Hong, J. Wang, X. Qu, X. Zhu, J. Liu, and J. Xiao, “Quantum convolutional neural network on protein distance prediction,” in _2021 International Joint Conference on Neural Networks (IJCNN)_.   IEEE, 2021, pp. 1–8.
- \[26\] M. Hibat-Allah, M. Ganahl, L. E. Hayward, R. G. Melko, and J. Carrasquilla, “Recurrent neural network wave functions,” _Physical Review Research_, vol. 2, no. 2, p. 023358, 2020.
- \[27\] C. Roth, “Iterative retraining of quantum spin models using recurrent neural networks,” 2020.
- \[28\] J. J. Hopfield, “Neural networks and physical systems with emergent collective computational abilities,” _Proceedings of the national academy of sciences_, vol. 79, no. 8, pp. 2554–2558, 1982.
- \[29\] H. Tang, Z. Feng, Y.-H. Wang, P.-C. Lai, C.-Y. Wang, Z.-Y. Ye, C.-K. Wang, Z.-Y. Shi, T.-Y. Wang, Y. Chen _et al._, “Experimental quantum stochastic walks simulating associative memory of hopfield neural networks,” _Physical Review Applied_, vol. 11, no. 2, p. 024020, 2019.
- \[30\] F. Li, S. Zhao, and B. Zheng, “Quantum neural network in speech recognition,” in _6th International Conference on Signal Processing, 2002._, vol. 2.   IEEE, 2002, pp. 1267–1270.
- \[31\] F. Li and G. Xu, “Quantum bp neural network for speech enhancement,” in _2009 Asia-Pacific Conference on Computational Intelligence and Industrial Applications (PACIIA)_, vol. 2.   IEEE, 2009, pp. 389–392.
- \[32\] L. Fu and J. Dai, “A speech recognition based on quantum neural networks trained by ipso,” in _2009 International Conference on Artificial Intelligence and Computational Intelligence_, vol. 2.   IEEE, 2009, pp. 477–481.
- \[33\] K. Mitarai, M. Negoro, M. Kitagawa, and K. Fujii, “Quantum circuit learning,” _Physical Review A_, vol. 98, no. 3, p. 032309, 2018.
- \[34\] Y. LeCun, Y. Bengio, and G. Hinton, “Deep learning,” _nature_, vol. 521, no. 7553, pp. 436–444, 2015.
- \[35\] X. Qu, J. Wang, and J. Xiao, “Enhancing data-free adversarial distillation with activation regularization and virtual interpolation,” in _ICASSP 2021-2021 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP)_.   IEEE, 2021, pp. 3340–3344.
- \[36\] J. Liu, J. Wang, X. Qu, B. Chen, Z. Wei, and J. Xiao, “Automatic joint optimization of algorithm-level compression and compiler-based acceleration with reinforcement learning for dnn in edge devices,” in _2021 International Joint Conference on Neural Networks (IJCNN)_.   IEEE, 2021, pp. 1–8.
- \[37\] N. Killoran, T. R. Bromley, J. M. Arrazola, M. Schuld, N. Quesada, and S. Lloyd, “Continuous-variable quantum neural networks,” _Physical Review Research_, vol. 1, no. 3, p. 033063, 2019.
- \[38\] D. P. Kingma and J. Ba, “Adam: A method for stochastic optimization,” in _ICLR (Poster)_, 2015.
- \[39\] L. Li, J. Wang, X. Qu, and J. Xiao, “Communication-memory-efficient decentralized learning for audio representation,” in _2021 International Joint Conference on Neural Networks (IJCNN)_.   IEEE, 2021, pp. 1–8.
- \[40\] W. Dai, C. Dai, S. Qu, J. Li, and S. Das, “Very deep convolutional neural networks for raw waveforms,” in _2017 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP)_.   IEEE, 2017, pp. 421–425.
- \[41\] Y. Tang, J. Wang, X. Qu, and J. Xiao, “Contrastive learning for improving end-to-end speaker verification,” in _2021 International Joint Conference on Neural Networks (IJCNN)_.   IEEE, 2021, pp. 1–7.
- \[42\] Z. Hong, J. Wang, W. Wei, J. Liu, X. Qu, B. Chen, Z. Wei, and J. Xiao, “When hearing the voice, who will come to your mind,” in _2021 International Joint Conference on Neural Networks (IJCNN)_.   IEEE, 2021, pp. 1–6.
- \[43\] Y. Wang, R. Skerry-Ryan, D. Stanton, Y. Wu, R. J. Weiss, N. Jaitly, Z. Yang, Y. Xiao, Z. Chen, S. Bengio _et al._, “Tacotron: Towards end-to-end speech synthesis,” _Proc. Interspeech 2017_, pp. 4006–4010, 2017.
- \[44\] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez, L. Kaiser, and I. Polosukhin, “Attention is all you need,” in _NIPS_, 2017.
- \[45\] K. Ito _et al._, “The lj speech dataset,” 2017.
- \[46\] P. Warden, “Speech commands: A dataset for limited-vocabulary speech recognition,” 2018.
